# Copy values into `.env.local` for local Expo runs or configure them as EAS
# environment variables for hosted builds. `EXPO_PUBLIC_*` values ship in the
# mobile app bundle.
EXPO_PUBLIC_REVENUECAT_IOS_API_KEY=
EXPO_PUBLIC_REVENUECAT_ANDROID_API_KEY=
